(function () {
    var $usernameFld, $passwordFld, $verifyPasswordFld;
    var $registerBtn;
    var userService = new UserService();
    $(main);

    function main() { … }
    function register() { … }
})();



/*
* Use the sign in, the sign up, and user admin controller code samples to get started. The buttons should invoke event handlers logout() and updateProfile() in the controller, which delegate the actions to corresponding functions in the user service.
*
*
*/